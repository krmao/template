<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.xixi.fruitshop.android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".library.FSApplication"
        android:allowBackup="true"
        android:fullBackupContent="@xml/fs_backup_descriptor"
        android:icon="@drawable/fs_emo_im_happy"
        android:label="@string/fs_app_name"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        android:theme="@style/FSAppTheme.Light.NoActionBar"
        tools:ignore="GoogleAppIndexingWarning">

        <meta-data
            android:name="DEBUG"
            android:value="${DEBUG}" />
    </application>

</manifest>
