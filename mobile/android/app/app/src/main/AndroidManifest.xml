<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.smart.template">

    <!-- 网络 -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- 读写 SDCard -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <!-- 相机 -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera.autofocus" />
    <uses-permission android:name="android.permission.FLASHLIGHT" />
    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />

    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- 定位 -->
    <!--<uses-permission android:name="android.permission.LOCATION_HARDWARE" />-->
    <!--<uses-permission android:name="android.permission.ACCESS_LOCATION_EXTRA_COMMANDS" />-->
    <!--<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />-->
    <!--<uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />-->

    <application
        android:name=".FinalApplication"
        android:allowBackup="true"
        android:allowTaskReparenting="false"
        android:fullBackupContent="@xml/st_backup_descriptor"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/st_app_name"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        android:theme="@style/LaunchTheme"
        tools:ignore="GoogleAppIndexingWarning"
        tools:replace="android:label,android:allowBackup">

        <activity
            android:name="com.smart.template.home.tab.HomeTabActivity"
            android:configChanges="orientation|screenSize"
            android:launchMode="singleTask"
            android:theme="@style/STAppTheme.Light.NoActionBar"
            android:windowSoftInputMode="stateHidden|adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="DEBUG"
            android:value="${DEBUG}" />
        <meta-data
            android:name="VERSION_RN"
            android:value="${VERSION_RN}" />
    </application>

</manifest>
